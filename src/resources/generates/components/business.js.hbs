import { Injectable, Inject } from '@nestjs/common';
import { Repository } from 'typeorm';
import { {{ properCase name }} } from '../entity/{{ lowerCase name }}.entity';

@Injectable()
export class {{ properCase name }}Service {
  constructor(
    @Inject('{{ properCase name }}RepositoryToken')
    private readonly {{ lowerCase name }}Repo: Repository<{{ properCase name }}>
  ) {}

  root(): string {
    try {
      return 'Api was working...';
    } catch (error) {
      throw error;
    }
  }
  async findAll(): Promise<{{ properCase name }}[]> {
    try {
      const data = await this.{{ lowerCase name }}Repo.find();
      return data;
    } catch (error) {
      throw error;
    }
  }
}
